#ане на день рождения подарили n кубиков. Он с друзьями решил построить из них пирамиду. #
#Ваня хочет построить пирамиду следующим образом: на верхушке пирамиды должен находиться 1 кубик, на втором уровне — 1 + 2 = 3 кубика, на третьем — 1 + 2 + 3 = 6 кубиков, и так далее. Таким образом, на i-м уровне пирамиды должно располагаться 1 + 2 + ... + (i - 1) + i кубиков.


# Ждем количество кубиков
n = int(input())
# переменные
hg = 0  # Текущая высота пирамиды
cubes_needed_for_level = 0 # Кубики для текущего уровня
total_cubes_used = 0 # Всего использовано кубиков

# Цикл для построения пирамиды уровень за уровнем
while True:
    hg += 1 # Переходим на следующий уровень
    # Количество кубиков на i-м уровне: 1 + 2 + ... + i = i * (i+1) / 2
    cubes_needed_for_level = (hg * (hg + 1)) // 2
        
    # Проверяем, хватит ли кубиков для нового уровня
    if total_cubes_used + cubes_needed_for_level <=n: 
        total_cubes_used += cubes_needed_for_level
    else:
    # Если кубиков не хватает, предыдущий уровень был максимальным
    # Уменьшаем высоту, так как этот уровень не построить
        hg -= 1
        break
    
# Выводим максимальную высоту
print(hg)

#Как это работает: 
#Чтение ввода: Программа считывает общее количество кубиков \(n\).
#Итерация по уровням: Используется цикл while для добавления уровней, начиная с первого (высота 1).
#Расчет кубиков для уровня: На каждом шаге вычисляется, сколько кубиков нужно для текущего уровня  (используется #формула \(i* (i+1)/2\)).
#Проверка наличия кубиков: Проверяется, не превысит ли общее количество использованных кубиков \(n\).
#Остановка: #Если кубиков не хватает, цикл прерывается, а hg уменьшается на 1, чтобы получить максимальную возможную #высоту, которой удалось достичь.
#Вывод: Печатается максимальная высота hg



