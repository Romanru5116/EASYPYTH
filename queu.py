#написать программу, которая принимает список имён клиентов, представляющий очередь. В начале списка находятся клиенты, стоящие в конце очереди, а в конце списка — те, кто ближе к началу. Программа должна выводить текстовые сообщения по мере продвижения очереди:

#для клиента, чья очередь наступила, выводится сообщение:
#Клиент {name} пройдите к стойке!

                  
#для остальных клиентов выводится текст:

#{Имена_оставшихся_в_очереди} ожидайте!

                  
Имена оставшихся клиентов указываются через запятую в порядке их расположения в очереди

Дополнительные требования:

# Принимаем строку имен и превращаем её в список
clients = input().split()

# В списке первый элемент — конец очереди, последний — начало. 
# Для обработки в правильном порядке (от начала к концу) используем цикл с конца.
while clients:
    # Удаляем последнего клиента из списка (того, кто в начале очереди)
    current_client = clients.pop()
    
    # Выводим приглашение для текущего клиента
    print(f"Клиент {current_client} пройдите к стойке!")
    
    # Если в списке еще кто-то остался, выводим их имена
    if clients:
        # Список имен оставшихся нужно вывести в порядке их очереди. 
        # Так как в нашем списке 'clients' начало очереди в конце, 
        # для вывода используем срез с разворотом [::-1]
        remaining = ",".join(clients[::-1])
        print(f"{remaining} ожидайте!")
