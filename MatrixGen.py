#Напишите программу, которая создаёт матрицу размером N × M, состоящую только из числа 7.
#Формат входных данных:

Два целых числа N и M — количество строк и столбцов в матрице (1 ≤ N, M ≤ 100).
# Чтение входных данных
n, m = map(int, input().split())

# Создание матрицы размером N x M, заполненной семерками
matrix = [[7] * m for _ in range(n)]

# Вывод матрицы в виде списка списков
print(matrix)

# Вывод элементов матрицы в виде таблицы
for row in matrix:
    print(*row)
