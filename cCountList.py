@На вход вашей программе поступает положительное целое число n, а ваша задача вывести в порядке возрастания все цифры, которые встречались в этом числе, и напротив каждого также необходимо вывести сколько раз данная цифра встречалась в числе n
n=input()
# Создаем массив счетчиков для цифр 0-9
counts =[0] * 10
# Преобразуем число в строку, чтобы пройти по каждой цифре
for digit in str(n):
    counts[int(digit)] += 1
    
# Выводим результаты в порядке возрастания (индексы от 0 до 9)
for i in range(10):
  if counts[i] > 0:
        print(f"{i} {counts[i]}")






